{% extends 'base.html.twig' %}

{% block title %}Create author{% endblock %}

{% block body %}
    <div class="row pb-3 pt-2">
        <div class="col-sm-offset-2 col-sm-10">
            <a class="btn btn-primary" href="{{ path('author.index') }}">
                Return to author list
            </a>
        </div>
    </div>
    {{ form(form) }}
{% endblock %}